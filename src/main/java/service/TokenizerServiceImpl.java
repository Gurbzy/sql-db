package service;

import model.tokens.Token;

import java.util.List;

public class TokenizerServiceImpl implements TokenizerService{
    @Override
    public List<Token> tokenize(StringBuffer input) {
        return List.of();
    }

    @Override
    public List<Token> tokenize(String input) {
        return List.of();
    }
}
